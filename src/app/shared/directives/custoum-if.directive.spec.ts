import { CustoumIfDirective } from './custoum-if.directive';

describe('CustoumIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CustoumIfDirective();
    expect(directive).toBeTruthy();
  });
});
